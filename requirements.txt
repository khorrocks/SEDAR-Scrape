# --- SEDAR+ browser automation (the scraper engine) ---
# Pin known-working versions. undetected-chromedriver ships only an sdist; on
# very new setuptools its setup.py can fail to build a wheel -- install with:
#   pip install --no-build-isolation -r requirements.txt   (see Dockerfile)
undetected-chromedriver==3.5.5
selenium>=4.15
websockets>=12.0

# --- Web app (FastAPI API + UI + queue) ---
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6
# Postgres driver (used when DATABASE_URL points at Postgres; harmless otherwise)
psycopg2-binary>=2.9
# Optional in-process daily recheck scheduler (ENABLE_INPROCESS_CRON=true)
apscheduler>=3.10
